{% extends 'base.html.twig' %}

{% block title"Find all best pins" %}

{% block body %}
    <h1>{{ pluralize( pins|length, 'pin') }}</h1>
    {% for pin in pins %}
        <h1> {{ pin.title }}</art> 
        <p>{{ pin.description }}</p>
    {% endfor %}
{% endblock %}
