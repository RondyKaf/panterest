{% extends 'base.html.twig' %}

{% block title"Find all best pins" %}

{% block body %}
    <h1>{{ plura(pins|length) }} pin</h1>
    {% for pin in pins %}
        <article> {{ pin.title }}</article> 
        <p>{{ pin.description }}</p>
    {% endfor %}
{% endblock %}
