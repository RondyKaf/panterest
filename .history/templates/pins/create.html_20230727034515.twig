{% extends 'base.html.twig' %}

{% block title " %}

{% block body %}
    <article>
        <h1>{{ pin.title }}</h1>
        <p>{{ description }}</p>
    </article>
    <a href="{{ path('app_home') }}">Go Back</a>
{% endblock %}
