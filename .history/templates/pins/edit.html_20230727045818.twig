{% extends 'base.html.twig' %}

{% block title "Edit Pin#" ~ pin.id %}

{% block body %}
    <h1>Edit Pin {{ pin.id }}</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input href="submit" value="Edit Pin"></inpu>
    {{ form_end(form) }}
    
{% endblock %}